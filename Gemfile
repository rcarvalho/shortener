source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.7.1'

gem 'sinatra'
gem 'redis'

group :test do
  gem 'rack-test'
  gem 'rspec'
  gem 'mocha'
end
